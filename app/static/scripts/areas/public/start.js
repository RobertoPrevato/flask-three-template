//
// Start the application for the public area.
//
R("start", ["app"], function (app) {
  //implement common setup logic here, e.g. ajax call to fetch the menu that should be displayed for this user.
  return app.start();
});
